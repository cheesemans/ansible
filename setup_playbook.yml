- hosts: all
  become: false
  vars:
    user_home: "{{ lookup('ansible.builtin.env', 'HOME') }}"
    user: "{{ lookup('ansible.builtin.env', 'USER') }}"
    oh_my_zsh_path: "{{ lookup('ansible.builtin.env', 'ZSH') }}"

  pre_tasks:
    #  Adding ultradvorka/ppa seems to not be necessary on jammy, but maybe on focal and before?
    # - name: Add ppa:ultradvorka/ppa apt repository (needed for hstr)
    #   apt_repository:
    #     repo: ppa:ultradvorka/ppa
    #     state: present
    #   become: yes
    #   changed_when: false
    - name: Update apt repositories
      ansible.builtin.apt:
        update_cache: yes
      become: true
      changed_when: false

  tasks:
    - include_tasks: roles/base.yml
    - include_tasks: roles/git.yml
    - include_tasks: roles/starship.yml
    - include_tasks: roles/zsh.yml
    - include_tasks: roles/vim.yml
    - include_tasks: roles/docker.yml
    - include_tasks: roles/rust.yml
