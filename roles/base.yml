- name: Install some basic apt packages
  ansible.builtin.apt: 
    name:
      - bash-completion
      - curl
      - hstr
      - python3
      - python3-pip
      - python-is-python3
    update_cache: yes
  become: yes
