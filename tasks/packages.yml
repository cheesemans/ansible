- name: Install apt packages
  apt: name={{item}}
  with_items:
    - git
    - vim
    - hstr
    - bash-completion
    - zsh
    - curl

- name: Install snap packages
  snap: name={{item}}
  with_items:
    - starship
    - docker


