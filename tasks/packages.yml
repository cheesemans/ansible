- name: Install apt packages
  apt: name={{item}}
  with_items:
    - git
    - vim
    - hstr
    - bash-completion
    - zsh
    - curl
    - docker.io

- name: Install snap packages
  snap: name={{item}}
  with_items:
    - starship


